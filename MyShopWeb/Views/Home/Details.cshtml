@using CoreMode.ViewModels
@model CoreMode.Model.Product


@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*@section styles{

        <link href="~/Content/jquery.bootstrap-touchspin.min.css" rel="stylesheet" />
    }
    @RenderSection("styles", false)*@
<h2 class="headline">產品</h2>
<div class="row">
    <div class="col-md-8">
        <img class="img-fluid" src="~/Content/ProductImage/@Model.Image" />
    </div>
    <h3>@Html.DisplayFor(model => model.Name)</h3>
    <div class="col-md-4">
        <hr />
        <dl class="text-left">
            <dt>
                @Html.DisplayNameFor(model => model.Price)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Price)
            </dd>



            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>

        </dl>

      

        <div class="col-md-12">


            @*<input type="text" value="100" name="testing" class="input-sm" id="@Model.Id">*@
            @*@{
                Html.RenderPartial("InandDeView", new CartItemViewModel());
            }*@
            @Html.Action("InandDeView");


            @*<a href="@Url.Action("AddToCart", "ShoppingCart", new { Id = Model.Id })" class="btn btn-default">加入購物車</a>*@
        </div>

    </div>
</div>
<p>
    @Html.ActionLink("回首頁", "Index")
</p>
<div class="abc"  value ="@Model.Id">
    @Html.Hidden("Id", Model.Id)
</div>


@section scripts{
    <script src="~/Scripts/jquery.bootstrap-touchspin.min.js"></script>
    <script type="text/javascript">
        $(function () {
            var i = $("input[name ='testing']");
            i.TouchSpin({
                min: 0,
                max: 100,
                step: 1

            });
            //要用第二行的
            @*$(".abc").click(function () {
                $.ajax({
                    url:@Url.Action("AddToCart", "ShoppingCart") + $(this).id + i.val(),
                        method :"POST"
                });


            });*@


        });
    </script>

}

